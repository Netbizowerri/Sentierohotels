# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# VITE_GOOGLE_MAPS_API_KEY: Optional Google Maps API key for interactive map and directions
VITE_GOOGLE_MAPS_API_KEY=""

# PRIVYR_WEBHOOK_URL: Privyr CRM webhook endpoint for securely receiving room bookings and leads
# Kept strictly server-side to prevent exposure in client bundles or GitHub commits
PRIVYR_WEBHOOK_URL=""

# Email Notifications (Optional for direct Gmail / SMTP alerts to Admin)
ADMIN_NOTIFICATION_EMAIL="netbiz0925@gmail.com"
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
